Be careful with encodind. Windows console by default uses CP866. 
Programm was tested in Windows 11.
Tests in programm shows, that all classes were built right
To create jar file you need to use mvn install
To run programm use java -jar path
